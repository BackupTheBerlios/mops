mpkg-tools: MOPSLinux package system - mainenance tools
mpkg-tools: 
mpkg-tools: For now, this package contains only one tool - package builder. It
mpkg-tools:  marked aplha, because it's not completed yet (but is already usable).
mpkg-tools:  You can use it to build a package descriptions.